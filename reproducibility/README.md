# Reproducibility records

This directory is reserved for release-linked evidence rather than mutable production output.

- `paper_reproduction_matrix.md`: draft mapping from paper items to code and outputs.
- `checksums/`: final source-data and selected artifact checksums, generated only after the corresponding versions are frozen.

Do not store credentials, private paths, full restricted datasets, or unreviewed large outputs here.
