# Examples

The maintained minimal examples are synthetic CSV fixtures under `../tests/fixtures/`. They exercise DOI metadata updates and deterministic SMILES extraction without redistributing production source data.

Run them through:

```bash
bash workflows/run_smoke_test.sh
```

Compact end-to-end motif/model fixtures may be added only when their expected scientific invariants and redistribution status are documented.

