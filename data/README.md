# Extracted workflow inputs

This directory contains deterministic unique-SMILES extracts derived from `../dataset/`. The canonical extraction command is:

```bash
python ../scripts/extract_unique_smiles.py \
  --per-table-output-dir task_unique_smiles
```

These files are derived data and inherit the unresolved redistribution status described in `../DATA.md`.

